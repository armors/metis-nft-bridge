{
    "name": "nft-bridge",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "clean": "rimraf ./cache && rimraf ./artifacts",
        "compile": "yarn clean && npx hardhat compile && npx hardhat --network metis-local compile",
        "test": "npx hardhat test",
        "merge-bridge-L1": "sol-merger \"./contracts/L1/L1NFTBridge.sol\" ./build",
        "merge-bridge-L2": "sol-merger \"./contracts/L2/L2NFTBridge.sol\" ./build",
        "merge-deposit": "sol-merger \"./contracts/NFTDeposit.sol\" ./build",
        "merge": "npm run merge-bridge-L1 && npm run merge-bridge-L2 && npm run merge-deposit"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@metis.io/contracts": "^1.2.3",
        "@nomiclabs/hardhat-ethers": "^2.0.4",
        "@nomiclabs/hardhat-etherscan": "^3.0.3",
        "chai": "^4.3.4",
        "dotenv": "^12.0.4",
        "ethers": "^5.5.3",
        "hardhat": "^2.8.2",
        "rimraf": "^3.0.2",
        "sol-merger": "^3.1.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^4.4.2"
    }
}
